import React from "react";

const Header = () => {
  // console.log(stat);
  return (
    <div>
      <span>My Todo List</span>
      <span>React</span>
    </div>
  );
};

export default Header;
